{
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yaml up --build --watch",
    "dev:frontend": "docker compose -f docker-compose.dev.yaml up --build --watch caddy frontend",
    "dev:backend": "docker compose -f docker-compose.dev.yaml up --build --watch caddy backend",
    "dev:down": "docker compose -f docker-compose.dev.yaml down",
    "dev:logs": "docker compose -f docker-compose.dev.yaml logs -f"
  }
}
